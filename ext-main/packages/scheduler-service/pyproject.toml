[project]
name = "healthtech-rpa-scheduler"
version = "0.1.0"
description = "FastAPI микросервис: генерация умного расписания процедур"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["app/tests"]
pythonpath = ["."]

[tool.ruff]
target-version = "py311"
line-length = 100
